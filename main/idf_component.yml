## Managed dependencies (ESP-IDF Component Manager)
##
## Topway builds use the UART smart-display path and do not need LVGL or
## esp_lcd. Legacy LVGL LCD targets are hidden by default in Kconfig; only
## enable them after adding the matching managed display packages back here.
dependencies: {}
