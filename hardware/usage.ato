"""
ESP-IDMS — Usage Example Circuit

This is a minimal circuit that builds the `ESP_IDMS` module
with all components populated. Use this as the build target
for `ato build`.

Equivalent to the `default` build in ato.yaml.
"""

#pragma experiment("FOR_LOOP")
#pragma experiment("BRIDGE_CONNECT")
#pragma experiment("TRAITS")

import ElectricPower
from "esp-idms.ato" import ESP_IDMS

module Usage:
    board = new ESP_IDMS
